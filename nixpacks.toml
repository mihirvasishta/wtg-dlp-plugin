# Nixpacks build configuration
# Used by Railway if no Dockerfile is present (kept as fallback).

[phases.install]
cmds = ["python3 -m pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
